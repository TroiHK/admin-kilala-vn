/*
 * SideNavigation Messages
 *
 * This contains all the text for the SideNavigation component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SideNavigation';

export default defineMessages({
  users: {
    id: `${scope}.users`,
    defaultMessage: 'Users',
  },
});
